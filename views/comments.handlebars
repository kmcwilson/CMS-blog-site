<h1> Add Comment</h1>
<div class="card">
    <h2>{{blog.title}}</h2>
    <h3> by {{name}}</h3>
    <div class="card-content">
        <p>{{blog.post}}</p>
    </div>
    <div class="control">
        <h3>Comment</h3>
        <textarea id="blog-comment" class="textarea" placeholder="Blog Post"></textarea>
    </div>
    <button id="add-comment" class="button" type="submit">Add Comment</button>
</div>